
<%= render "/shared/banner" %>
<%= render "/shared/searchbar" %>
<div class = "container">
    <div class="cards">
      <% @hauntedhouses.each do |hauntedhouse| %>
      <%#affiche uniquement les maisons du proprio <% if hauntedhouse.user == current_user %>
      <% if hauntedhouse.user == current_user %>
          <% if hauntedhouse.photo.attached? %>
              <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path hauntedhouse.photo.key, height: 300, width: 400, crop: :fill %>')">
              <div>
                 <p><%= link_to hauntedhouse.name, hauntedhouse_path(hauntedhouse) %></p>
              </div>
              <div class ="tag">

                  <% if !current_user.nil? && current_user != hauntedhouse.user %>
                      <% if hauntedhouse.reserved?(current_user) %>
                        <p>Reservée</p>
                      <% end %>
                  <% end %>

              </div>

              </div>
        <% end %>
      <% end %>



<% end %>
</div>
<!-- <%= link_to new_hauntedhouse_path do %>
  <i class="fa fa-plus"> Add a new hauntedhouse</i>
<% end %><br />-->

</div>